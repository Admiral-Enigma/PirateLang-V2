do(
	ahoy(sqrt(9)),
	ahoy(*(6,6)),
	define(pythagoras, fun(a, b, do(
		*(a,a),
		*(b,b),
		define(res, +(a,b)),
		ahoy(sqrt(res))
	))),
	define(a1, int(in("Første tal: "))),
	define(b1, int(in("Andet tal: "))),
	pythagoras(a1,b1)
)
